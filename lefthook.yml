---
commit-msg:
  parallel: true
  commands:
    lint-commit-msg:
      run: pnpm commitlint --edit {1}

pre-commit:
  parallel: true
  commands:
    lint-staged:
      run: pnpm biome check --staged
