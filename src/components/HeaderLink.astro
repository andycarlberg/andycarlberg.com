---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"a">;

const { href, class: className, ...props } = Astro.props;
const pathname = Astro.url.pathname.replace(import.meta.env.BASE_URL, "");
const subpath = pathname.match(/[^/]+/g);
const isHome = href === "/" || href === "";
const isActive = isHome
  ? pathname === "/" || pathname === ""
  : href === pathname || href === `/${subpath?.[0] || ""}`;
---

<a
  href={href}
  aria-current={isActive ? "page" : undefined}
  class:list={[
    className,
    'font-display text-sm font-bold uppercase tracking-wider transition-all pb-1 border-b-2',
    isActive 
      ? 'text-primary border-primary' 
      : 'text-on-surface-variant border-transparent hover:text-on-background hover:border-outline-variant',
  ]}
  {...props}
>
  <slot />
</a>
